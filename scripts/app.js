let currency = '$';

const entryPointsForm = Object.assign({}, form);
const exitPointsForm = Object.assign({}, form);

const entryPoints = entryPointsForm.createForm('Точки входа');
const exitPoints = exitPointsForm.createForm('Точки выхода');